from telegram.ext import filters, ContextTypes
from telegram.ext import ContextTypes, ContextTypes
async async def corso_google_suite_livello_2_handler(update, context):

    user = update.effective_user

    text = (

        "🌐 *Google Suite – Livello 2*\n\n"

        "Google Documenti: scrivere testi collaborativi. Fogli Google: prime tabelle, gestione dati semplice."

    )

    await context.bot.send_message(chat_id=user.id, text=text)
