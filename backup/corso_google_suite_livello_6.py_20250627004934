from telegram.ext import filters, ContextTypes
from telegram.ext import ContextTypes, ContextTypes
async async def corso_google_suite_livello_6_handler(update, context):

    user = update.effective_user

    text = (

        "🌐 *Google Suite – Livello 6*\n\n"

        "Collegare Google Suite con altri tool (Zapier, Notion, CRM), workflow di team, sicurezza dati."

    )

    await context.bot.send_message(chat_id=user.id, text=text)
