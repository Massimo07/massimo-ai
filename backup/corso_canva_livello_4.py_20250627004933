from telegram.ext import filters, ContextTypes
from telegram.ext import ContextTypes, ContextTypes
async async def corso_canva_livello_4_handler(update, context):

    user = update.effective_user

    text = (

        "🎨 *Canva & Social Visual – Livello 4*\n\n"

        "Brand kit Canva: carica loghi, palette colori, modelli personalizzati. Programma i post social dal calendario integrato."

    )

    await context.bot.send_message(chat_id=user.id, text=text)
