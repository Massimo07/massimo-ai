from telegram.ext import filters, ContextTypes
from telegram.ext import ContextTypes, ContextTypes
async async def corso_analytics_livello_2_handler(update, context):

    user = update.effective_user

    text = (

        "📊 *Analytics – Livello 2*\n\n"

        "Prime statistiche: crescita personale, visualizza andamento iscrizioni e vendite mese per mese."

    )

    await context.bot.send_message(chat_id=user.id, text=text)
