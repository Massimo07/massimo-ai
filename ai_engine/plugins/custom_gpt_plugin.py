# ai_engine/plugins/custom_gpt_plugin.py
class CustomGPTPlugin:
    def predict(self, prompt, **kwargs):
        # Integrazione reale qui (es. openai)
        return {"output": f"Risposta GPT custom: {prompt}"}
